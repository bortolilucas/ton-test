import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  flatListContent: {
    paddingHorizontal: 25,
    paddingTop: 20,
  },
  title: {
    fontSize: 20,
    marginBottom: 12,
  },
});
